<Window x:Class="WpfApp1.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        xmlns:common="clr-namespace:WpfApp1.Common"
        mc:Ignorable="d" Name="LoginWindow"
        Title="登录界面" Height="400" Width="300" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"  WindowStyle="None"
        AllowsTransparency="True" Background="{x:Null}" >
    
    <Border Margin="5" Background="#EFEFEF" >
        <Border.Effect>
            <DropShadowEffect Color="Gray" ShadowDepth="0" BlurRadius="5" Opacity="0.2" Direction="0"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <Label VerticalAlignment="Top" Height="20" MouseDown="Label_MouseDown"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right" Grid.Row="0">
                <Button x:Name="ButtonMini" Style="{StaticResource MinimizeWindowButtonStyle}" Command="{Binding MinWindowCommand}" CommandParameter="{Binding ElementName=LoginWindow}"/>
                <Button x:Name="ButtonClose" Style="{StaticResource CloseWindowButtonStyle}" Command="{Binding CloseWindowCommand}" CommandParameter="{Binding ElementName=LoginWindow}"/>
            </StackPanel>
            <StackPanel Grid.Row="1" Orientation="Vertical">
                <Label Content="环境样品库数据管理系统" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="20" Margin="0 40 0 0"/>
                <Label Content="欢迎登录" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="14" TextElement.Foreground="Blue" Margin="0 25 0 0"/>
            </StackPanel>
            <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Width="170" Grid.Row="2">
                <Label Content="用户名" VerticalContentAlignment="Center"  Width="50"/>
                <TextBox x:Name="TextBoxUsername" TextWrapping="Wrap"  Width="120" Style="{DynamicResource TextBoxUsernameStyle}" Text="{Binding userModel.UserName}"/>
            </StackPanel>
            <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Width="170" Grid.Row="3">
                <Label Content="密码" VerticalContentAlignment="Center"  Width="50"/>
                <PasswordBox x:Name="PasswordBoxPassword" Width="120"
                             common:PasswordBoxHelper.Attach="True" common:PasswordBoxHelper.Password="{Binding userModel.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Width="170" Grid.Row="4">
                <Button x:Name="ButtonLogin" Content="登录" Width="75" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=LoginWindow}" Margin="0 0 20 0"/>
                <Button x:Name="ButtonSignin" Content="注册" Width="75" Command="{Binding SigninCommand}" CommandParameter="{Binding ElementName=LoginWindow}"/>
            </StackPanel>
            <Image Source="../Assets/Images/logo-hjxy_700_74.png" Margin="0 10 0 0" Grid.Row="5"/>
        </Grid>
    </Border>
</Window>
